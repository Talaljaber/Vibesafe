import type * as vscode from "vscode";

// VibeGuard VS Code Extension — placeholder
// Phase 7 will implement:
//   - Activity bar sidebar with deploy status card
//   - Issues tree view (grouped by severity)
//   - Problems panel diagnostics
//   - Hover provider for inline explanations
//   - Commands: scan, rescan, copyFixPrompt, applyFix
//   - Workspace Trust integration

export function activate(_context: vscode.ExtensionContext): void {
  // Phase 7 implementation
}

export function deactivate(): void {
  // Phase 7 cleanup
}
